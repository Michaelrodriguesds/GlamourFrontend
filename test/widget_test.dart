// Teste básico do app — verifica que a splash screen carrega
import 'package:flutter_test/flutter_test.dart';

void main() {
  // Os testes completos requerem dotenv e Riverpod inicializados.
  // Para testar, rode: flutter test --plain-name "smoke"
  test('placeholder', () {
    expect(true, isTrue);
  });
}